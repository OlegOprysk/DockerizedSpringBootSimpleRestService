spring:
  application:
    name: springBoot
  cloud:
    bus:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false
config: Local Test property
app:
  common:
    variable: Local Test property [app.common.variable]
  test:
    prop: Local Test propert [app.common.test.prop]